<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gaocheng.riversystem">


    <application
        android:name=".RiverSystemApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme"
        tools:replace="android:icon, android:theme"
        >
        <!--配置小米8全面屏 会出现黑屏-->
        <meta-data android:name="android.max_aspect" android:value="2.1" />
        <activity android:name=".RiverMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.RegulationUpdateActivity"/>
        <activity android:name=".ui.activity.RegulationOnSiteAssessmentActivity"/>
    </application>

</manifest>